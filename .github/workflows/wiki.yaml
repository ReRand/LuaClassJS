name: wiki publish
on: 
  push:
    paths:
      - wiki/**
      - .github/workflows/wiki.yaml
jobs:
  publish_wiki:
    runs-on: ubuntu-latest
    steps:
      - name: publish wiki
        uses: actions/checkout@v3
      #  - run: cd ${wiki}
      - run: echo "${wiki/test}"
      - run: git commit -m {wiki}
